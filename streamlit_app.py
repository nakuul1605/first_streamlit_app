import streamlit
streamlit.title('My parents new healthy diner')
streamlit.header('In header function')
streamlit.text('in text function')
streamlit.text('in text function')
streamlit.text('in text function')
streamlit.text('in text function')
streamlit.text('in text function')
streamlit.text('in text function')
